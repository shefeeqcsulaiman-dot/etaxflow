# TaxFlow Test Run - 2026-05-19

## Scope

This run validated the current local development stack:

- Frontend Vite build
- Live frontend asset loading
- Live FastAPI API availability
- Authenticated API route sweep
- Isolated backend workflow checks with FastAPI TestClient
- Live SQLite database integrity checks
- Small local API latency smoke

This is not yet a full production QA certification. The repository does not currently include formal pytest, Playwright, k6, OWASP ZAP, tenant-isolation, or Celery worker test suites.

## Environment

```text
Frontend: http://127.0.0.1:5173
Backend:  http://127.0.0.1:8010
Database: backend/taxflow-seed50-v2.db
Date:     2026-05-19
```

## Results

| Area | Result | Notes |
| --- | --- | --- |
| Frontend build | Pass | `npm run build` completed successfully. |
| Frontend live assets | Pass | `/`, `/src/main.jsx`, and `/taxflow/index.html` returned HTTP 200. |
| Backend import | Pass | `app.main` imported successfully with 73 routes. |
| API docs/OpenAPI | Pass | `/docs` and `/openapi.json` returned HTTP 200. |
| Auth smoke | Pass | Admin login returned 200; missing token on `/auth/me` returned 401. |
| Simple GET route sweep | Pass | 41/41 authenticated simple GET routes returned HTTP 200. |
| Isolated workflow tests | Pass | 13/13 workflow checks passed against a temporary SQLite DB. |
| Live DB integrity | Pass | `PRAGMA integrity_check` returned `ok`; foreign key check returned 0 rows. |
| Journal balance check | Pass | No unbalanced journals found in live DB. |
| Local latency smoke | Pass | Tested endpoints returned 200 with acceptable local response times. |
| Backend bytecode compile | Blocked | `compileall` failed because Windows denied writes to existing `__pycache__` files. Runtime import still passed. |

## Isolated Workflow Checks

Executed against a temporary SQLite database, not the live seed DB:

```text
PASS auth login
PASS auth rejects missing token
PASS auth/me with token
PASS invoice create
PASS invoice VAT total: subtotal 200.00, VAT 10.00, total 210.00
PASS unbalanced journal rejected
PASS balanced journal accepted
PASS source transaction create
PASS source transaction validate
PASS source transaction approve queues posting
PASS tax line created on approval
PASS audit log created on approval
PASS missing account approval rejected
```

The test DB cleanup was blocked by a Windows file lock/permission issue after SQLAlchemy used the file.

## Live API GET Sweep

All 41 simple authenticated GET endpoints passed. Key examples:

```text
/api/v1/auth/me
/api/v1/companies/current
/api/v1/accounts
/api/v1/invoices
/api/v1/source-transactions
/api/v1/tax/codes
/api/v1/tax/lines
/api/v1/reports/dashboard
/api/v1/reports/trial-balance
/api/v1/exceptions
/api/v1/events
/api/v1/inventory/mappings
/api/v1/inventory/valuation-layers
/api/v1/payroll/employees
/api/v1/corporate-accounting/summary
```

## Live Database Counts

```text
companies                  1
users                      1
accounts                   10
tax_codes                  4
invoices                   0
invoice_lines              0
journal_entries            0
journal_lines              0
source_transactions        100
source_transaction_lines   100
posting_jobs               0
tax_lines                  0
audit_logs                 850
exception_events           0
domain_events              0
event_outbox               0
app_data_records           468
```

## Local Latency Smoke

20 sequential requests per endpoint:

| Endpoint | Statuses | Avg ms | P95 ms | Max ms |
| --- | --- | ---: | ---: | ---: |
| `/reports/dashboard` | 200 | 62.49 | 72.81 | 77.94 |
| `/app-data` | 200 | 79.59 | 105.92 | 170.41 |
| `/source-transactions` | 200 | 29.64 | 37.20 | 38.33 |
| `/purchases` | 200 | 33.73 | 40.44 | 51.74 |

## Gaps

The following cannot be honestly marked complete yet:

- Full Playwright UI behavior testing
- True concurrent load testing with k6/Locust
- OWASP ZAP security scan
- Tenant isolation testing with multiple tenants/users
- Celery queue worker retry testing
- Production PostgreSQL transaction tests
- Period locking workflow tests
- Inventory BOX to PCS workflow tests against durable inventory movement APIs
- Full VAT return workflow with posted tax lines in the live seed DB
- Duplicate invoice prevention, because the current invoice model has no unique constraint on invoice number per company
- Exception Center deep simulation, because the live seed has no exception records

## Highest Priority Fixes Before Production QA

1. Add formal pytest tests for accounting, VAT, source transactions, and auth.
2. Add Playwright tests for login and core UI workflows.
3. Add test tenants/users so tenant isolation can be proven.
4. Add deterministic seed data for invoices, journals, tax lines, inventory movements, posting jobs, and exceptions.
5. Add duplicate invoice constraints and tests.
6. Add k6 or Locust scripts for posting/report load tests.
